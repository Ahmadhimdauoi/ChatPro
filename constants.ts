export const APP_TITLE = 'ChatPro';

// Backend API URL (replace with your actual backend URL)
export const API_BASE_URL = 'http://localhost:5000/api';

// WebSocket URL (replace with your actual Socket.IO backend URL)
export const WS_URL = 'http://localhost:5000'; // Socket.IO typically connects to the base HTTP URL